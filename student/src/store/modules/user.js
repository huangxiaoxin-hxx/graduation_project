const state = {
  userInfo: null
}

const getters = {
  
}

const actions = {
  
}

const mutations = {
  setUserInfo(state, value) {
    state.userInfo = value
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}