const state = {
  categories: []
}

const getters = {
  
}

const actions = {
  
}

const mutations = {
  setCategories(state, value) {
    state.categories = value
  }
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}